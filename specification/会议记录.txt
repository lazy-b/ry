例：
2017.07.20：
更新部分：
    新增了specification文件夹，内部包含“更新说明”以及“项目规范”两个文件。
    “项目规范”包含一些合作开发的建议，以及一些git的常用目的
    “更新说明”则包含了一些不方便在项目提交上的说明
要求：
    全体成员阅读“项目规范”，
    
2017.07.20:
前端任务：
    1、订单进度表的增删改查路径
    2、订单进度表新增（或修改）页面表单的字段验证
    3、订单进度表新增（或修改）页面表单增加日期选择器
    4、订单进度表时间的毫秒数改为日期
    5、订单表新增（或修改）页面表单增加日期选择器
    6、订单进度表和订单表页面缩小导致搜索栏错位的bug
    7、订单进度表新增（或修改）页面表单的字段的状态输入改为下拉列表
后端任务：
    1.新增订单信息表，及订单进度表的数据字典。
    2.调试订单进度增加产生的bug
共同任务：
    1、共同确定日期展示形式以及输入形式
           建议：2017-06-26 20:06:29这种形式；
                      展示表中的时间分两部分
    2、共同确定状态输入的下拉列表的可选值
           答：数字字典中写入可选择。
           
 2017-7-21：
 首先需要做的事情：
    1.文件名的命名规则；
    2.数据库字典确认；
    2.需求文档；
 1.数据备份：
    1、一种数据（例如订单表）在数据库中拥有两张表。一张用于日常增删改查操作，另一张用于持久化存储或用于数据分析。
    2、根据表的操作频次，确定备份频率以及日常操作表的大小。  
 2.数据表设计
    1.新增 文件名操作表： 字段：ID，fileName,createBy，modifyBy,createTime,modifyTime用途：用于区分重复的文件、按文件名操作数据库，提示是否遗漏上传及重复;
    2.所有表都增加fileNameID字段,不为空。
    3.用户用表单增加的数据也需要输入文件名（前端用列表处理）;
 3.数据展示
    1.数据按id大小降序排列，再按默认字段排序；（找涛哥确认）；
    2.所有数据按文件名为单位，进行分页查询。
 前端任务：
    1、订单进度表的增删改查路径
    2、订单进度表新增（或修改）页面表单的字段验证
    3、订单进度表新增（或修改）页面表单增加日期选择器
    4、订单进度表时间的毫秒数改为日期
    5、订单表新增（或修改）页面表单增加日期选择器
    6、订单进度表和订单表页面缩小导致搜索栏错位的bug
    7、订单进度表新增（或修改）页面表单的字段的状态输入改为下拉列表
    8、更改订单进度查询的查询url
    9、页面排序
    10、订单信息表验证出问题
    11、多条件查询
    12、订单页面新增文件名展示
    13、表单的完善（订单进度日历）
后端任务：
    1.确认数据库字典，重构数据库。
    2.调试订单进度增加产生的bug
    3.完成按文件名批次做CRUD操作。
    4.excel导入bug的调整。
    5.excel导入功能的完善。
         
           
           